// Este fichero puede ser reemplazado durante la compilación usando el array `fileReplacements`.
// `ng build` reemplaza `environment.ts` por `environment.prod.ts`.
// La lista de archivos reemplazados se encuentra en `angular.json`.

export const environment = {
  production: false,
  endpoint: 'http://localhost:3001/'
};

/*
 * Para facilitar la depuración en modo desarrollo, puedes importar el siguiente fichero
 * para ignorar los marcos de pila de error relacionados con la zona como `zone.run`, `zoneDelegate.invokeTask`.
 *
 * Esta importación debe ser comentada en modo producción porque tendrá un impacto negativo en el rendimiento si se lanza un error.
 * en el rendimiento si se produce un error.
 */
// import 'zone.js/plugins/zone-error'; // Incluido con Angular CLI.
